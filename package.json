{
  "name": "core-registry-api",
  "version": "1.0.19",
  "description": "Combined Carbon Core Registry API Application",
  "main": "index.js",
  "type": "commonjs",
  "bin": "index.js",
  "chiaVersion": "2.0.1",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "fs-extra": "^11.1.1",
    "jest": "^29.7.0",
    "pkg": "^5.3.0",
    "tar": "^6.1.15",
    "wait-on": "^7.2.0",
    "yauzl": "^2.10.0"
  },
  "pkg": {
    "assets": [
      "package.json",
      "index.js"
    ],
    "outputPath": "dist"
  },
  "scripts": {
    "start": "node index.js",
    "build": "pkg .",
    "test": "concurrently \"npm run start\" \"wait-on $CORE_REGISTRY_HOST:31310  && wait-on $CORE_REGISTRY_HOST:31313 && wait-on $CORE_REGISTRY_HOST:31311 && jest\"",
    "test-dev": "concurrently \"npm run start\" \"wait-on localhost:31310 && wait-on localhost:31313 && wait-on localhost:31311 && cross-env CORE_REGISTRY_HOST=localhost jest\"",
    "prepare-binary": "rm -rf dist && mkdir dist",
    "create-win-x64-dist": "npm run prepare-binary && pkg package.json -t node18-win-x64 --out-path dist",
    "create-mac-x64-dist": "npm run prepare-binary && pkg package.json -t node18-macos-x64 --out-path dist",
    "create-linux-x64-dist": "npm run prepare-binary && pkg package.json -t node18-linux-x64 --out-path dist",
    "create-linux-arm64-dist": "npm run prepare-binary && pkg package.json -t node18-linux-arm64 --out-path dist"
  },
  "keywords": [
    "chia-blockchain"
  ],
  "author": "Michael Taylor",
  "license": "Apache-2.0",
  "dependencies": {
    "superagent": "^8.1.2"
  }
}
